# 아람출퇴근 v0.22

정류장 헤더 심플화 버전입니다.

- 상단 출근/퇴근 탭만 모드 표시로 사용
- 본문 내부의 중복 `출근` / `퇴근` 텍스트 제거
- 새안의원 정류장 / 주안역환승정류장 앞 아이콘 제거
- 본문 우측 `실시간` 배지 제거
- 정류장명 + 정류장 번호만 남기고 세로 여백 축소
- 버스 카드, 실시간 API, 20초 자동갱신, 429 대응 로직은 v0.21 그대로 유지
- PWA 캐시 버전을 v0.22로 갱신

기존 Git 연결 폴더에 app/public/package.json/README.md를 덮어쓴 뒤 push 하세요.
