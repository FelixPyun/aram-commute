# 아람출퇴근 v0.20

실사용 UI 정리 버전입니다.

- 상단 `아람출퇴근` 타이틀 중앙 정렬
- 자동 갱신 시간도 타이틀 아래 중앙 정렬
- 우측 상단 새로고침 버튼 유지
- 노선 카드별 중복 `실시간` 배지 제거
- 모바일 카드 내부 세로 여백 약 10~15% 축소
- 하단 중복 새로고침 버튼 제거
- 하단 인천 BIS 실시간 연결 상태는 유지
- v0.19의 API 호출 제한 대응 로직 유지

기존 Git 연결 폴더에 app/public/package.json/README.md를 덮어쓴 뒤 push 하세요.
